Summarization Service Quarkus application

inspired by : https://docs.quarkiverse.io/quarkus-langchain4j/dev/quickstart-summarization.html#

Build and run in dev mode:
```bash
mvn quarkus:dev
```

[WIP] Build and run in production mode:
```bash
mvn clean package
java @/path/to/tornado-argfile /target/quarkus-app/quarkus-run.jar
```